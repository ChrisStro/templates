---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whoami
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik-system
  hostnames:
    - "whoami.local.127.0.0.1.sslip.io"
  rules:
    - backendRefs:
        - name: whoami
          port: 80
